node_modules/
*.sqlite
*.sqlite-wal
*.sqlite-shm
*.sqlite-journal
.doctor-test.*
.DS_Store
out/
tmp/

# build artifacts (regenerated by app/build.sh)
app/OmnigenVault.app/
app/build-number
app/Paths.swift

# web SPA build output (regenerated by `npm run build`)
web/dist/
app/AppIcon.iconset/
app/AppIcon.icns
app/icon-1024.png
app/candidates/

# generated galleries / vault data (live on the external disk, never in git)
gallery.html
preview.html
images/
thumbs/
quarantine/

# AI agent / session data — never publish (Code Brain, Claude, Codex, Antigravity, hooks)
/.ai/
/.claude/
/.agents/
/.codex/
/.githooks/
/.mcp.json
/CLAUDE.md
/AGENTS.md
/bootstrap-code-brain.sh
scripts/env-check.sh
scripts/preflight.sh
